#include "main.h"
#include <stdio.h>

/**
 * main - check the code.
 *@c: character to check
 * Description: checks for uppercase character
 * Return: 0
 */
int _isupper(int c)
{

char c;

if (c >= 'A' && c <= 'Z')
{
return (0);
}
else
{
return (0);
}

}
